# 동시성 해결 보고서 (강성규)

> **프로젝트:** 치과 예약 및 결제 시스템 (DentalLink)  
> **개선 날짜:** 2025-11-05  
> **개선 대상:** pointAccount 포인트 입출금 및 Reservation 도메인 예약 생성 api

-----

## 개선 목표

포인트의 입출금과 병원 예약 생성 시 발생하는 동시성 문제(Concurrency Issue)를 
해결하여 데이터 무결성과 비즈니스 로직의 일관성을 확보

## 동시성 문제란
동시성 문제(Concurrency Issue)란, 
여러 사용자가 동시에 같은 데이터에 접근하거나 수정할 때 발생하는 비정상적인 동작을 의미

## 문제 발생 예시
- 두 사용자가 거의 동시에 포인트 충전 요청을 보낼 때, DB의 같은 point_account 대해 증감 연산이 중복 실행되어, 한쪽 결과가 덮어쓰여 불일치 상태가 발생할 수 있음 

- 여러 사용자가 같은 병원, 같은 시간으로 예약 요청을 한다면, DB에는 중복된 appointmentDate가 저장되어 한 시간대에 여러 명이 예약되는 문제가 발생한다.

## 동시성 문제의 확인
- PointAccountLockTest 및 ReservationLockTest class 테스트 코드를 통해 동시성 문제가 발생한다는 것을 명확히 함

## 개선 방향
1. 포인트 입출금 동시성 제어
- 비관적 락(Pessimistic Lock) 적용
- pointAccount repository의 findByUserIdWithLock() 및 findByIdWithLock() 메서드를 통해 트랜잭션 시작 시점에 DB에 락을 건다. 
- 이를 통해 한 시점에 한 스레드만 해당 포인트 계좌를 수정할 수 있도록 제어함 

2. 병원 예약 생성 동시성 제어 
- Redisson 기반 분산 락 (DistributedLockAspect) 적용
- 병원별·예약 시간별로 고유한 락을 설정하여, 동시에 여러 사용자가 같은 시간대에 예액을 시도하더라도 단 한 건만 처리되도록 제어함

## 해당 락 선정 이유
1. 포인트 입출금 동시성 제어
- 포인트 계좌는 단일 데이터베이스 에 존재하며, 동시에 여러 스레드가 동일한 pointAccount을 수정할 수 있음
- 이러한 경우 DB 레벨에서의 동시성 제어가 필요하다 생각했기에 분산락보다 비관락을 선택
- 또한 포인트는 트래픽이나 속도보다 정확성이 더 중요한 영역이기에 낙관락보다 비관락을 선택

2. 병원 예약 생성 동시성 제어 
- 예약 생성은 이후 서버 여러 인스턴스에서 동시에 호출될 수 있는 가능성이 존재
- 따라서 단일 DB 락으로는 정확한 제어가 어려움
- 또한 여러 서버 간에도 동일한 자원(병원+시간대)에 대해 하나의 공통된 락을 공유할 수 있기에 다른 락보다 분산락을 선택 

## 코드 적용 방식
1. 포인트 입출금 동시성 제어

```java
package com.dentallink.domain.pointAccount.repository;
import com.dentallink.domain.pointAccount.entity.PointAccount;
import jakarta.persistence.LockModeType;
import org.springframework.data.jpa.repository.*;
import org.springframework.data.repository.query.Param;
public interface PointAccountRepository extends JpaRepository<PointAccount, Long> {
    // 기본 조회 (락 없음)
    Optional<PointAccount> findByUserId(Long userId);
    
    // 비관적 락 (userId 기준)
    @Lock(LockModeType.PESSIMISTIC_WRITE)
    @Query("SELECT p FROM PointAccount p WHERE p.user.id = :userId")
    Optional<PointAccount> findByUserIdWithLock(@Param("userId") Long userId);

    // 비관적 락 (accountId 기준)
    @Lock(LockModeType.PESSIMISTIC_WRITE)
    @Query("SELECT p FROM PointAccount p WHERE p.id = :id")
    Optional<PointAccount> findByIdWithLock(@Param("id") Long id);
}
```
- 해당 락을 사용해 트랜잭션이 시작되는 순간 대상 포인트 계좌 데이터에 락을 즉시 설정함
- 이로써 동일한 다른 스레드의 접근이 차단되어 동시에 여러 요청이 들어와도 정확히 한 번만 포인트가 수정되도록 제어함

2. 병원 예약 생성 동시성 제어 
- build.gradle에 Redisson 의존성 추가 (implementation 'org.redisson:redisson-spring-boot-starter:3.27.2')
- DistributedLockAspect 클래스 및 DistributedLock 인터페이스 제작
- ReservationInternalService의 createReservation 클래스에 @DistributedLock 어노테이션 적용 (병원 ID와 예약 시간을 조합한 키)
- DistributedLockAspect를 통해 Redis에 병원별·예약 시간별 고유한 락 키를 등록해, 이를 통해 여러 서버 인스턴스에서 동시에 동일한 자원에 접근하더라도 오직 하나의 요청만 예약 생성 로직에 진입하도록 제어

## 동시성 제어 확인
- 개선된 로직의 동작을 검증하기 위해 동시성 테스트 코드를 작성하여 수행함 PointAccountLockTest에서는 동시에 여러 스레드가 포인트를 입출금해도 최종 잔액이 정확히 일치함을 확인
- ReservationLockTest 에서는 동일 병원·동일 시간대에 여러 사용자가 동시에 예약을 시도했을 때 오직 한 건만 성공하고, 나머지는 락 획득 실패 예외로 처리되는 것을 확인 

## 결론 
- 비관적 락과 분산 락이 정상적으로 동작하며,데이터 정합성과 예약 단일성이 보장됨을 검증함