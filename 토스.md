# 토스 api 트러블 슈팅 (성규)

> **프로젝트:** 치과 예약 및 결제 시스템 (DentalLink)  
> **개선 날짜:** 2025-11-01  
> **트러블 슈팅:** Toss Payments 결제 연동 중 시행착오 기록

## 문제 배경
초기에는 단순히 payment 내부적으로 포인트를 증감시키는 방식으로 결제를 처리했다.
이 방식은 테스트에는 편했지만, 실제 결제 흐름을 반영하지 못했기 때문에 Toss API를 연동하기로 했다.

하지만 처음 Toss API를 적용할 때는 ready → 사용자 결제 -> confirm / cancel 흐름이나,
Toss에서 제공하는 결제 HTML UI 사용 방법,
그리고 orderId 중복 방지의 중요성을 잘 몰라 여러 시행착오가 발생했다.

## 당시 내가 몰랐던 점들
### 1. orderId는 고유해야 한다. 
- 처음엔 orderId를 단순히 "string"이나 "ORDER-1" 처럼 고정된 겂으로 보냈다.
- 하지만 Toss는 orderId를 기준으로 결제를 구분하기에, 동일한 orderId로 여러 번 결제를 시도하면, 이미 처리된 주문으로 인식하기에 결제가 거절된다.
- 이 문제로 인해 confirm 단계에서 "이미 결제된 주문입니다" 라는 응답을 계속 받았다.
- 이는 Toss 뿐만 아니라 어떤 주문을 할때도 orderId는 고유해야 한다는 점을 몰랐던 잘못이었다.

#### 해결 방식
- 아래와 같은 방식으로 매번 고유한 'orderId'를 생성하도록 했다.
```java
body: JSON.stringify({
    amount: totalAmount,
    orderId: "ORDER-" + Date.now(), // 매번 고유 ID 생성
})
```

### 2. Toss에서 직접 결제 UI를 제공한다.
- 처음에는 벡엔드에서 결제만 처리하면 되는 줄 알았다. 
- 하지만 Toss는 HTML 형태의 결제창을 직접 제공하여 해당 html을 통해 프론트 작업을 해야 한다는 것을 알았고,
- 즉 나는 처음에 벡엔드의 ready 요청이 성공하면 결제가 준비완료가 되니, 이후 confirm을 호출하면 결제가 끝난다고 생각했다. 
- 하지만 toss에서의 결제는 ready -> 사용자가 결제창에서 실제 결제를 진행 -> confirm의 순서로 이루어진다.
- ready 요청으로 결제가 발생하는 것이 아닌, toss checkout 페이지을 프론트에 직접 띄어줘야 사용자가 결제를 진행할 수 있었다.
- 이걸 몰랐기에 "결제가 실제로 이루어지지 않은 주문"이라는 실패 응답을 받았다.

#### 해결 방식
- toss에서 제공하는 위젯 스크립트를 활용해 payment.html을 제작했다.
- ready 요청 후 toss 위젯을 띄워서 사용자 결제 -> 결제 완료 후 successUrl 이동 -> 벡엔드 confirm 요청으로 승인 처리 흐름으로 변경했다. 

### 3. successUrl / failUrl 의 역할을 몰랐다.
- 처음에는 ready → confirm 순서만 알고, 결제 후에 사용자가 어떤 화면으로 이동해야 하는지를 신경 쓰지 않았다.
- 하지만 Toss는 결제가 완료되면 반드시 successUrl(성공 시) 또는 failUrl(실패 시) 로 리다이렉트한다.
- 나는 이걸 설정하지 않아서 결제창이 닫히지 않거나, 결제 후 아무 화면도 표시되지 않았다.
- 또한 Toss는 결제 완료 시, successUrl로 paymentKey, orderId, amount를 쿼리스트링으로 보내준다.
- 처음엔 이 과정을 몰라서 결제창은 뜨는데, 결제 결과가 백엔드에 반영되지 않아 “결제 완료”로 표시되지 않았다.

#### 해결 방식
- 이를 위해 success.html, fail.html 파일을 제작하여 사용자가 결제 완료 후 이동하도록 구성했다.
- 또한 아래와 같이 success.html에서 는 URL 파라미터를 읽어 자동으로 /confirm API를 호출하여 결제 승인 및 상태 표시를 수행하도록 했다.

```java
// Toss에서 전달된 결제 정보 추출
const urlParams = new URLSearchParams(window.location.search);
const paymentKey = urlParams.get("paymentKey");
const orderId = urlParams.get("orderId");
const amount = parseInt(urlParams.get("amount"));

// 백엔드로 결제 승인 요청
        const response = await fetch("http://localhost:8080/api/payments/confirm", {
    method: "POST",
            headers: {
        "Content-Type": "application/json",
                "Authorization": `Bearer ${token}`,
    },
    body: JSON.stringify({ paymentKey, orderId, amount }),
});

```
