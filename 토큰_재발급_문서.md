# RefreshToken 구현 보고서 (김제경)

> **프로젝트:** 치과 예약 및 결제 시스템 (DentalLink)  
> **개선 날짜:** 2025-11-02  
> **개선 대상:** Login 시 Refresh token 발급
---
## 개선 목표
AccessToken이 탈취당했을 때를 대비하여 Token의 기한을 줄이고, 
RefreshToken을 사용하여 AccessToken을 재발급 받을 수 있도록 합니다.

## RefreshToken 사용 이유
기존 방식대로 AccessToken 만을 발급할 경우,
탈취 시 치명적입니다. <br>
따라서, AccessToken의 수명을 줄이고 RefreshToken을 사용하여 간접적으로 AccessToken을 만료시킬 수 있도록 합니다.

## 기능 설명
RedisDao 클래스를 사용하여 RefreshToken을 캐시에 저장합니다.
재발급 요청이 있을 시, 사용자의 Header 속 refresh-token 값을 읽어 캐시 저장소 안 재발급 토큰과 비교한 뒤,
**기존 AccessToken을 폐기하고** 새 토큰을 발급합니다.

### 로그인
 - 로그인 시 AccessToken과 함께 refresh-token을 발급합니다.
 - 발급한 refresh-token은 캐시 저장소에 저장됩니다.
### 토큰 재발급
 - 사용자 Header 에서 refresh-token 값을 읽어 유효한 재발급 토큰인지 검증합니다.
 - 유효한 재발급 토큰이라면, 기존에 쓰이던 AccessToken을 만료시키고 새 AccessToken을 발급합니다.
### 로그아웃
 - refresh-token을 캐시 저장소에서 삭제합니다.